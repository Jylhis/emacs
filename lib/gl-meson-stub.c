/* Placeholder translation unit so the stub libgnu used during the
   Meson migration is non-empty.  Phase 2 of the migration replaces
   the surrounding stub with a real gnulib build and removes this
   file.  See .claude/plans/migrate-from-current-build-replicated-gadget.md
   for the migration plan.  */

int _gl_meson_stub_unused;
